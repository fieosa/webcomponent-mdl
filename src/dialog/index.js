import { basicClassCreator } from '../utils';

export const MaterialDialogContent = basicClassCreator('mdl-dialog__content');
export const MaterialDialogTitle = basicClassCreator('mdl-dialog__title');
export { default as MaterialDialog } from './dialog';
export { default as MaterialDialogActions } from './dialog-actions';
